import { Flex } from '@mantine/core'
import { Form } from '../components/Form/Form'

export const Jobs = () => {
  return (
    <Flex style={{ width: '70%', margin: '0 auto' }}>
      <Form />
      <Flex>

      </Flex>
    </Flex>
  )
}
